# 农历纪念日功能测试指南

## 快速测试步骤

### 测试1: 添加农历纪念日
1. 在浏览器中打开 `index.html`
2. 点击左侧边栏"纪念日 · 节日"旁的 ✎ 编辑按钮
3. 在弹出的对话框中：
   - 输入名称：`测试农历节日`
   - 输入表情：`🏮`（可选）
   - 点击"农历"按钮（切换历法类型）
   - 选择农历月份：`正月`
   - 选择农历日期：`初一`
   - 选择一个颜色
   - 点击"添加"按钮

### 预期结果
- ✅ 纪念日列表中应该出现新添加的项目
- ✅ 日期显示为："农历正月初一 · 每年"
- ✅ 左侧边栏"纪念日 · 节日"下方应该显示这个纪念日

### 测试2: 编辑农历纪念日
1. 在纪念日列表中，找到刚才添加的农历纪念日
2. 点击右侧的 ✎ 编辑按钮
3. 观察表单是否正确显示：
   - 历法类型应该自动切换到"农历"
   - 农历月份和日期应该正确显示
4. 修改月份为`八月`，日期为`十五`
5. 点击"更新"按钮

### 预期结果
- ✅ 日期应该更新为："农历八月十五 · 每年"
- ✅ 所有修改都应该保存

### 测试3: 混合显示
1. 添加一个公历纪念日：
   - 点击编辑按钮
   - 输入名称：`测试公历节日`
   - 表情：`🎉`
   - 确保选择的是"公历"
   - 选择日期：今天的日期
   - 点击"添加"

### 预期结果
- ✅ 侧边栏应该同时显示公历和农历纪念日
- ✅ 按照下次发生时间排序
- ✅ 点击纪念日可以跳转到对应日期

### 测试4: 删除功能
1. 在纪念日列表中点击 🗑 删除按钮
2. 确认删除

### 预期结果
- ✅ 纪念日应该被删除
- ✅ 侧边栏和列表都应该更新

## 常见问题排查

### Q: 点击"农历"按钮没有反应
**检查**: 
- 打开浏览器控制台（F12）
- 查看是否有JavaScript错误
- 确保 `openSpecialModal()` 中绑定了事件

### Q: 农历月份/日期下拉框是空的
**检查**:
- `buildLunarSelects()` 是否被调用
- 控制台是否有错误

### Q: 保存后日期显示 "undefined"
**检查**:
- `saveSpecial()` 是否正确处理 `spCalType === 'lunar'` 的情况
- 数据中是否正确保存了 `lunarMonth` 和 `lunarDay`

### Q: 编辑农历纪念日时显示公历日期
**检查**:
- `loadSpecialIntoForm()` 是否检查 `sp.calendar === 'lunar'`
- 是否调用了 `setCalType('lunar')`

## 开发者工具调试

打开浏览器控制台（F12），输入以下命令查看数据：

```javascript
// 查看所有纪念日
__cal.store.getSpecialDays()

// 查看今天的纪念日（包括农历匹配）
__cal.store.getSpecialDaysForDate(new Date())

// 测试农历转公历（例如：2024年农历八月十五）
// 注意：这个函数在全局作用域外，需要在代码中查看
```

## 数据持久化

数据保存在浏览器的 localStorage 中：
```javascript
// 查看原始数据
JSON.parse(localStorage.getItem('apple-calendar:v1'))
```

---

如果遇到问题，请检查浏览器控制台的错误信息！
